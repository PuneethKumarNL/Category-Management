import React from "react";

const Modal = () => {
  return (
    <div>
      <h2>This is the popup content!</h2>
      <p>More content inside the modal.</p>
      <button onClick="">Close</button>
    </div>
  );
};

export default Modal;
